# FrameSceneExtractor

FrameSceneExtractor is a Windows desktop application that detects major visual scene changes in a video and exports one representative still image for each detected scene, together with start and end timestamps.

The application is designed to be installed through a single Windows `Setup.exe`. The installed application contains its own .NET runtime plus bundled FFmpeg/FFprobe binaries, so end users do not need to install Python, .NET, FFmpeg, Node.js, OpenCV, or other runtime dependencies.

> Initial application source is being added to this repository.
